# Conditional Dashboard Route - Flow Diagram

## 🔄 User Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    User visits /                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│         ConditionalDashboardRoute Component                  │
│                                                              │
│  1. Check AuthContext.loading                               │
│  2. Check AuthContext.user                                  │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
┌──────────────┐          ┌──────────────┐
│   loading    │          │   !loading   │
│   = true     │          │   = false    │
└──────┬───────┘          └──────┬───────┘
       │                         │
       ▼                         │
┌──────────────┐                 │
│    Show      │                 │
│   Loading    │                 │
│   Spinner    │                 │
└──────────────┘                 │
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
            ┌──────────────┐          ┌──────────────┐
            │  user exists │          │  user = null │
            │ (Authenticated)         │   (Guest)    │
            └──────┬───────┘          └──────┬───────┘
                   │                         │
                   ▼                         ▼
        ┌──────────────────┐      ┌──────────────────┐
        │   Redirect to    │      │  Render Dashboard│
        │   /dashboard     │      │   at / (root)    │
        └──────┬───────────┘      └──────┬───────────┘
               │                         │
               ▼                         ▼
    ┌──────────────────┐      ┌──────────────────┐
    │ URL: /dashboard  │      │    URL: /        │
    │ Content: Dashboard│      │ Content: Dashboard│
    └──────────────────┘      └──────────────────┘
```

## 🔀 Route Handling

```
┌─────────────────────────────────────────────────────────────┐
│                    Route Configuration                       │
└─────────────────────────────────────────────────────────────┘

Route: "/"
├─ Component: ConditionalDashboardRoute
├─ Guest: Render Dashboard (no redirect)
└─ Authenticated: Redirect to "/dashboard"

Route: "/dashboard"
├─ Component: Dashboard
├─ Guest: Accessible ✅
└─ Authenticated: Accessible ✅

Route: "/login"
├─ Component: Login (wrapped in GuestRoute)
├─ Guest: Accessible ✅
└─ Authenticated: Redirect to "/dashboard"

Route: "/riwayat-transaksi"
├─ Component: TransactionHistory (wrapped in ProtectedRoute)
├─ Guest: Redirect to "/login" ❌
└─ Authenticated: Accessible ✅
```

## 🎭 User Journey Examples

### Journey 1: Guest Browsing

```
1. Guest visits https://www.canvango.com/
   └─> URL: /
   └─> Content: Dashboard
   └─> No redirect

2. Guest clicks "BM Accounts"
   └─> URL: /akun-bm
   └─> Content: BM Accounts page
   └─> Accessible

3. Guest clicks "Transaction History"
   └─> URL: /login (redirected)
   └─> Content: Login page
   └─> Protected route
```

### Journey 2: User Login

```
1. Guest at https://www.canvango.com/
   └─> URL: /
   └─> Content: Dashboard

2. Guest clicks "Login"
   └─> URL: /login
   └─> Content: Login page

3. User enters credentials and submits
   └─> Authentication successful
   └─> Redirect to /dashboard

4. User now at https://www.canvango.com/dashboard
   └─> URL: /dashboard
   └─> Content: Dashboard (with user data)
```

### Journey 3: Authenticated User Navigation

```
1. User visits https://www.canvango.com/
   └─> Auto-redirect triggered
   └─> URL: /dashboard
   └─> Content: Dashboard

2. User clicks "Transaction History"
   └─> URL: /riwayat-transaksi
   └─> Content: Transaction History
   └─> Accessible (authenticated)

3. User manually types https://www.canvango.com/
   └─> Auto-redirect triggered again
   └─> URL: /dashboard
   └─> Content: Dashboard
```

## 🔐 Authentication State Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    AuthContext State                         │
└─────────────────────────────────────────────────────────────┘

Initial Load
├─ loading: true
├─ user: null
└─> Show loading spinner

Auth Check Complete (Guest)
├─ loading: false
├─ user: null
└─> Allow guest access

Auth Check Complete (Authenticated)
├─ loading: false
├─ user: { id, username, role, ... }
└─> Trigger redirect to /dashboard

Session Expired
├─ loading: false
├─ user: null
└─> Revert to guest behavior
```

## 🛡️ Route Protection Matrix

| Route | Guest Access | Auth Access | Redirect Behavior |
|-------|-------------|-------------|-------------------|
| `/` | ✅ Dashboard | ✅ → `/dashboard` | Conditional |
| `/dashboard` | ✅ Dashboard | ✅ Dashboard | None |
| `/login` | ✅ Login | ❌ → `/dashboard` | GuestRoute |
| `/register` | ✅ Register | ❌ → `/dashboard` | GuestRoute |
| `/akun-bm` | ✅ Products | ✅ Products | None |
| `/riwayat-transaksi` | ❌ → `/login` | ✅ History | ProtectedRoute |
| `/top-up` | ❌ → `/login` | ✅ TopUp | ProtectedRoute |
| `/admin/*` | ❌ → `/login` | ✅ (admin only) | ProtectedRoute |

## 🔄 Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                      main.tsx                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              AuthProvider                              │ │
│  │  ┌──────────────────────────────────────────────────┐ │ │
│  │  │           BrowserRouter                          │ │ │
│  │  │  ┌────────────────────────────────────────────┐ │ │ │
│  │  │  │            Routes                          │ │ │ │
│  │  │  │  ┌──────────────────────────────────────┐ │ │ │ │
│  │  │  │  │  /login → GuestRoute → Login        │ │ │ │ │
│  │  │  │  └──────────────────────────────────────┘ │ │ │ │
│  │  │  │  ┌──────────────────────────────────────┐ │ │ │ │
│  │  │  │  │  /* → MemberArea                    │ │ │ │ │
│  │  │  │  │    ┌──────────────────────────────┐ │ │ │ │ │
│  │  │  │  │    │  MemberRoutes               │ │ │ │ │ │
│  │  │  │  │    │  ┌────────────────────────┐ │ │ │ │ │ │
│  │  │  │  │    │  │ / → Conditional       │ │ │ │ │ │ │
│  │  │  │  │    │  │     DashboardRoute    │ │ │ │ │ │ │
│  │  │  │  │    │  └────────────────────────┘ │ │ │ │ │ │
│  │  │  │  │    │  ┌────────────────────────┐ │ │ │ │ │ │
│  │  │  │  │    │  │ /dashboard → Dashboard│ │ │ │ │ │ │
│  │  │  │  │    │  └────────────────────────┘ │ │ │ │ │ │
│  │  │  │  │    └──────────────────────────────┘ │ │ │ │ │
│  │  │  │  └──────────────────────────────────────┘ │ │ │ │
│  │  │  └────────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```
